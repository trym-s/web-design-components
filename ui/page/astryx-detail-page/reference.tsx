/* Use when: Single-record detail in two columns: a summary header with status and actions, a repeating line-item list, a totals block that sums it, and a chronological activity timeline in the rail. */

export { default } from "./upstream/demo";
