/* Use when: Reference landing that is pure wayfinding: a banner above a grid of category cards linking into sections, with no article content on the surface itself. */

export { default } from "./upstream/demo";
