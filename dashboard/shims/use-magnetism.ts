/* Shim — upstream's cursor-magnetism hook was not captured. The stub keeps the
   call signature so the component mounts; the drift is simply absent. */
export function useMagnetism(_ref: unknown, _strength?: number): void {}
