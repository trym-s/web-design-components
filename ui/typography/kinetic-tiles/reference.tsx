/* Use when: a letterform should ripple as a grid of tiles each riding its own wave. */

"use client";

import { KineticACard } from "./src/kinetic-a/KineticACard";

export default function Reference() {
  return <KineticACard />;
}
