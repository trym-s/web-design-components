/* Use when: a word or shape needs a motion-trail smear built from stacked copies instead of a blur filter. */

"use client";

import { SmearCard } from "./src/smear/SmearCard";

export default function Reference() {
  return <SmearCard />;
}
