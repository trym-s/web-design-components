/* Use when: a hero needs thousands of live glyphs vortexing into a wordmark, with a CRT pass on top. */

"use client";

import { SwirlPlayground } from "./src/swirl/playground";

export default function Reference() {
  return <SwirlPlayground />;
}
