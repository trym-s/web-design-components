/* Use when: Four-column nested navigation narrowing left to right (team workspace, channel list, message stream, thread panel) where each column scopes the next. */

export { default } from "./upstream/demo";
