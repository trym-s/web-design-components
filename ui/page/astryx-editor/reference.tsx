/* Use when: Three-pane authoring workspace: a source palette on one flank, a live canvas in the middle, and an inspector that retargets to whatever is selected. */

export { default } from "./src/demo";
