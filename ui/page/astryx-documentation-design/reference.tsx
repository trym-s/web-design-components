/* Use when: Reference article alternating prose with live rendered examples: an interactive preview, a property table, paired do and don't comparisons, and copyable code blocks. */

export { default } from "./upstream/demo";
