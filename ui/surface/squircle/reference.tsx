/* Use when: a corner radius must ease smoothly like Apple's squircle instead of kinking where the arc meets the edge. */

"use client";

import { SquirclePlayground } from "./src/squircle/playground";

export default function Reference() {
  return <SquirclePlayground />;
}
