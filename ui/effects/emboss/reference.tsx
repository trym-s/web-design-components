/* Use when: text or an SVG mark should read as pressed into plaster, metal, or paper with controllable light and depth. */

"use client";

import { EmbossPlayground_ } from "./src/emboss/playground";

export default function Reference() {
  return <EmbossPlayground_ />;
}
