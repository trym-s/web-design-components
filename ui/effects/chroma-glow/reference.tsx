/* Use when: a headline or mark needs real bloom plus a warm/cool chromatic-aberration split that leans toward the cursor. */

"use client";

import { ChromaGlowPlayground } from "./src/chroma/playground";

export default function Reference() {
  return <ChromaGlowPlayground />;
}
