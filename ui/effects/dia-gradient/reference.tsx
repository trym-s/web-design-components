/* Use when: a page or panel needs a soft rainbow floor glow that rises on load, Dia-browser style. */

"use client";

import { DiaGradient } from "./upstream/dia-gradient/standalone/DiaGradient";

export default function Reference() {
  return <DiaGradient />;
}
