/* Use when: two or more cards should fuse with an inward-bending gooey joint instead of sitting as separate rectangles. */

"use client";

import { LiquidPlayground } from "./src/liquid/playground";

export default function Reference() {
  return <LiquidPlayground />;
}
